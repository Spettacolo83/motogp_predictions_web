# Auth.js Secret - Generate with: npx auth secret
AUTH_SECRET=

# Google OAuth (optional - for Google login)
AUTH_GOOGLE_ID=
AUTH_GOOGLE_SECRET=

# Invitation code required for new user registration
INVITATION_CODE=motogp_2026

# Trust host (set to true in production)
AUTH_TRUST_HOST=true

# Base URL (required for email verification links in production)
AUTH_URL=http://localhost:3001

# SMTP Configuration (optional - emails logged to console if not set)
SMTP_HOST=
SMTP_PORT=587
SMTP_USER=
SMTP_PASS=
EMAIL_FROM=noreply@motogp-predictions.com
